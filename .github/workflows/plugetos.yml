name: PlugetOS Nested Arch Build

on: 
  workflow_dispatch: 

jobs:
  qemu-arch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@a5ac7e51b41094c92402da3b24376905380afc29

      - name: Install QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 qemu-utils wget

      - name: Download Arch ISO
        run: |
          # En güncel ISO'yu çekiyoruz
          wget https://mirror.rackspace.com/archlinux/iso/latest/archlinux-x86_64.iso -O arch.iso

      - name: Create Virtual Disk
        run: qemu-img create -f qcow2 pluget_disk.qcow2 10G

      - name: Boot Arch via QEMU
        run: |
          # 'timeout' ekleyerek sonsuz döngüden kaçınıyoruz
          # 'no-reboot' ile hata aldığında kapanmasını sağlıyoruz
          timeout 10m qemu-system-x86_64 \
            -m 2G \
            -smp 2 \
            -display none \
            -vga none \
            -drive file=pluget_disk.qcow2,format=qcow2 \
            -cdrom arch.iso \
            -boot d \
            -net nic -net user \
            -serial stdio \
            -no-reboot
